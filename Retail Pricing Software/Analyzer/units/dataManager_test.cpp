/*
 * dataManager_test.cpp
 *
 *  Created on: Feb 23, 2019
 *      Author: kraychik
 */

/*
#include "gtest/gtest.h"
#include "CDataManager.h"


TEST(ProjectInfo, Init) {
	CProjectInfo projectInfo(100);
	projectInfo.LoadSalesHistory();
	auto salesHistory = projectInfo.GetSalesHistory();
	EXPECT_DOUBLE_EQ(692.0, salesHistory->Income());
}

TEST(DataManager, Init) {
	CDataManager *dataManager = CDataManager::GetInstance();
	dataManager->LoadProject(100);
	auto salesHistory = dataManager->GetProjectInfo(100)->GetSalesHistory();
	EXPECT_DOUBLE_EQ(692.0, salesHistory->Income());
}
*/
